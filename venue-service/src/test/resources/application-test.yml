spring:
  datasource:
    url: jdbc:h2:mem:venue_service;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
management:
  tracing:
    enabled: false
